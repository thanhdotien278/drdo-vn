NODE_ENV=development
PORT=4005
MONGODB_URI=mongodb://127.0.0.1:27017/drdo
CORS_ORIGIN=http://localhost:5173
# JWT signing secret — dev default exists, but always set a real secret outside dev.
JWT_SECRET=drdo-dev-secret-change-me
JWT_EXPIRES_IN=7d
